'use strict';const assert=require('node:assert'),fs=require('node:fs'),os=require('node:os'),path=require('node:path'),{appendLedgerEntry,verifyLedgerFile}=require('../scripts/lib/release-authorization-ledger'),{evaluatePromotion}=require('../scripts/lib/promotion-policy-engine');const d=fs.mkdtempSync(path.join(os.tmpdir(),'p65-')),p=path.join(d,'l.jsonl'),authorization={authorizationId:'a',authorized:true};appendLedgerEntry({ledgerPath:p,authorization});const v=verifyLedgerFile(p),r=evaluatePromotion({policy:{environments:{qa:{allowedSources:['development'],requiredChecks:['buildPassed'],requiredApprovalRoles:['qa'],changeTicketRequired:false,maximumRiskScore:75}}},request:{releaseId:'r',sourceEnvironment:'development',targetEnvironment:'qa',riskScore:10,evidence:{buildPassed:true},approvals:[{role:'qa'}]},authorization,ledgerVerification:v});assert.strictEqual(r.authorized,true);assert.strictEqual(r.certificate.ledgerHeadHash,v.headHash);console.log('Phase 65 integration tests passed.');
