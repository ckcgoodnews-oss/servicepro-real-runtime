const assert=require('assert');const fs=require('fs');const path=require('path');const {createServiceMarketplaceRepository}=require('../apps/api/src/repositories/serviceMarketplaceRepository');
let state={serviceMarketplaceItems:[{id:'plumbing',name:'Plumbing Pack',status:'published'},{id:'hvac',name:'HVAC Pack',status:'published'}],serviceMarketplaceInstallations:[]};const store={type:'json',read:()=>structuredClone(state),write:data=>{state=structuredClone(data);}};const marketplace=createServiceMarketplaceRepository(store);
assert.strictEqual(marketplace.listCatalog().length,2);const installation=marketplace.install('tenant_a',{itemId:'hvac',installedBy:'owner'});assert.strictEqual(marketplace.listInstallations('tenant_a').length,1);assert.strictEqual(marketplace.listInstallations('tenant_b').length,0);assert.strictEqual(marketplace.uninstall('tenant_b',installation.id),false);assert.strictEqual(marketplace.uninstall('tenant_a',installation.id),true);
const read=file=>fs.readFileSync(path.join(__dirname,'..',file),'utf8');const component=read('apps/web/src/components/MarketplaceWorkspace.tsx');for(const label of ['All service industries','Service packs','Connectors','Themes','Every service industry'])assert.match(component,new RegExp(label));const seed=read('apps/api/src/data/serviceMarketplaceCatalog.js');for(const trade of ['Plumbing Operations Pack','HVAC Service Pack','Carpet & Upholstery Pack','Landscaping Operations Pack'])assert.match(seed,new RegExp(trade.replace(/[&]/g,'&')));assert.match(read('packages/database/postgres/727_marketplace_experience.sql'),/service_marketplace_installations/);
console.log('Sprint 727 marketplace experience test passed.');
