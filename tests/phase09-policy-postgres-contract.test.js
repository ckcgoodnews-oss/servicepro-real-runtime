const assert=require('assert');const{createPolicyManagementRepository}=require('../apps/api/src/repositories/policyManagementRepository');const calls=[],responses=[];const a={type:'postgres',query:async(sql,params)=>{calls.push({sql:sql.replace(/\s+/g,' '),params});return responses.shift()||{rows:[],rowCount:0}}};a.transaction=async f=>f(a);const r=createPolicyManagementRepository(a);(async()=>{const id='00000000-0000-4000-8000-000000000001';responses.push({rows:[],rowCount:0});assert.strictEqual(await r.createVersion({tenantId:'b',policyId:id,content:'x'}),null);assert.match(calls[0].sql,/p.tenant_id=\$1 AND p.id=\$2::uuid/);responses.push({rows:[],rowCount:0});assert.strictEqual(await r.submitVersion('b',id),null);responses.push({rows:[],rowCount:0});assert.strictEqual(await r.publishVersion('b',id),null);assert.strictEqual(calls.length,3);responses.push({rows:[],rowCount:0});assert.strictEqual(await r.createAcknowledgement({tenantId:'b',policyId:id,versionId:id,subjectId:'u'}),null);assert.match(calls[3].sql,/v.tenant_id=p.tenant_id/);console.log('Phase 9 policy PostgreSQL contract test passed.')})().catch(e=>{console.error(e);process.exit(1)});
