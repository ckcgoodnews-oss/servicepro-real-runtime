const assert=require('assert');const{createEnterpriseSecretsRepository}=require('../apps/api/src/repositories/enterpriseSecretsRepository');let data={};const repo=createEnterpriseSecretsRepository({type:'json',read:()=>data,write:x=>{data=x}});const secret=repo.createSecret({tenantId:'a',name:'API',type:'api_key',owner:'ops',vaultRef:'vault://one',actor:'u1'});assert.strictEqual(repo.schedule('b',secret.id,'2027-01-01','evil'),null);assert.strictEqual(data.secretAuditEvents.length,1);assert.ok(repo.schedule('a',secret.id,'2027-01-01','u1'));assert.strictEqual(repo.begin('b',secret.id,'evil'),null);assert.strictEqual(data.secretAuditEvents.length,2);assert.ok(repo.begin('a',secret.id,'u1'));assert.strictEqual(repo.complete('b',secret.id,'vault://evil','evil'),null);assert.ok(repo.complete('a',secret.id,'vault://two','u1'));assert.strictEqual(repo.revoke('b',secret.id,'evil','evil'),null);assert.ok(repo.revoke('a',secret.id,'retired','u1'));console.log('Phase 9 enterprise secrets tenant-isolation test passed.');
