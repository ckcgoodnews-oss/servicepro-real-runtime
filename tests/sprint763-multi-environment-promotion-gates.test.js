'use strict';const assert=require('node:assert'),{evaluatePromotion}=require('../scripts/lib/promotion-policy-engine');const policy={environments:{production:{allowedSources:['staging'],requiredChecks:['securityScanPassed','rollbackPlanValidated'],requiredApprovalRoles:['release-manager','security','sre'],changeTicketRequired:true,maximumRiskScore:20}}};const ok=evaluatePromotion({policy,request:{releaseId:'r1',sourceEnvironment:'staging',targetEnvironment:'production',changeTicket:'CHG-1',riskScore:10,evidence:{securityScanPassed:true,rollbackPlanValidated:true},approvals:[{role:'release-manager'},{role:'security'},{role:'sre'}]},authorization:{authorized:true,authorizationId:'a1'},ledgerVerification:{valid:true,headHash:'h1'}});assert.strictEqual(ok.authorized,true);const no=evaluatePromotion({policy,request:{sourceEnvironment:'qa',targetEnvironment:'production',riskScore:50,evidence:{},approvals:[]},authorization:{authorized:false},ledgerVerification:{valid:false}});assert.strictEqual(no.authorized,false);console.log('Sprint 763 multi-environment promotion gate tests passed.');
