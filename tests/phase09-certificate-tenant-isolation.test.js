const assert=require('assert');const{createCertificateLifecycleRepository}=require('../apps/api/src/repositories/certificateLifecycleRepository');let data={};const repo=createCertificateLifecycleRepository({type:'json',read:()=>data,write:x=>{data=x}});const domain=repo.createDomain({tenantId:'a',name:'a.test',owner:'ops'});assert.strictEqual(repo.createCertificate({tenantId:'b',domainId:domain.id,commonName:'a.test',owner:'ops',expiresAt:'2027-01-01'}),null);const cert=repo.createCertificate({tenantId:'a',domainId:domain.id,commonName:'a.test',owner:'ops',expiresAt:'2027-01-01'});assert.strictEqual(repo.activate('b',cert.id),null);assert.strictEqual(data.certificateEvents.length,0);assert.ok(repo.activate('a',cert.id));assert.strictEqual(repo.rotate('b',cert.id,'vault://forged'),null);assert.strictEqual(data.certificateEvents.length,1);assert.ok(repo.rotate('a',cert.id,'vault://next'));console.log('Phase 9 certificate tenant-isolation test passed.');
