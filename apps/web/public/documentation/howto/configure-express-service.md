# Configure Express Service

The Express Service API and tenant settings are available, but the subscriber administration screen and customer form are not yet published. Keep Express Service disabled until those screens and file-upload validation are deployed.

Implemented safeguards include tenant-specific eligible services, description/photo requirements, urgency controls, and a dependency that prevents required deposits unless Customer Payments and connected-account charges are enabled.
