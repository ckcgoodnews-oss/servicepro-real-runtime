# HOW TO — Configure Partial Payments

Open **Settings → Billing → Customer Payments**, select **Allow partial payments**, and choose **Save payment settings**. The API still calculates the outstanding invoice balance and rejects zero, negative, over-balance, or currency-mismatched payments.

Clear the option when every online payment must cover the complete balance.
