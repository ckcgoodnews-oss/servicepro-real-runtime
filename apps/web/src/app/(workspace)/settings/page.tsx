import Link from 'next/link';

export const metadata={title:'Settings'};
const settings=[['Profile and security','Personal details, password, MFA, sessions, and API tokens.','/profile'],['Organization','Business units, departments, locations, teams, and reporting structure.','/organization'],['Notifications','Inbox, email, browser push, dispatch, billing, and product preferences.','/notifications'],['Marketplace','Service-industry packs, connectors, extensions, and workspace themes.','/marketplace'],['Documentation','Operating guides, tutorials, API reference, and release notes.','/docs']];
export default function SettingsPage(){return <div className="dashboard-content settings-page"><div className="dashboard-intro"><div><p className="eyebrow"><span/> Workspace administration</p><h1>Settings</h1><p>Manage personal preferences, organization structure, communications, and installed capabilities.</p></div></div><section className="settings-grid">{settings.map(([title,description,href])=><Link className="panel" href={href} key={href}><span aria-hidden="true">→</span><h2>{title}</h2><p>{description}</p><strong>Open settings</strong></Link>)}</section></div>;}
