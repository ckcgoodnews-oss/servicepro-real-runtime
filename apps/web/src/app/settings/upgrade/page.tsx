import { UpgradeWorkspace } from '@/components/UpgradeWorkspace';

export const metadata = { title: 'Upgrade your plan — ServicePro' };

export default function UpgradePage() {
  return <UpgradeWorkspace />;
}
