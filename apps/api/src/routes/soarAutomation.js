const { sendJson } = require('../utils/http');
function repo(req){return req.context.repositories.soarAutomation} function filters(req){return{tenantId:req.body.tenantId||req.context.tenantId||'',status:req.body.status||''}} function wrap(p,res,status=200){Promise.resolve(p).then(data=>sendJson(res,status,{data})).catch(err=>sendJson(res,err.status||500,{error:{code:err.code||'error',message:err.message,details:err.details||{}}}))}
module.exports={createPlaybook(req,res){wrap(repo(req).createPlaybook({...req.body,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},listPlaybooks(req,res){wrap(repo(req).listPlaybooks(filters(req)),res)},activatePlaybook(req,res,id){wrap(repo(req).activatePlaybook(id),res)},disablePlaybook(req,res,id){wrap(repo(req).disablePlaybook(id),res)},createStep(req,res,playbookId){wrap(repo(req).createStep({...req.body,playbookId,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},listSteps(req,res,playbookId){wrap(repo(req).listSteps(playbookId),res)},createConnector(req,res){wrap(repo(req).createConnector({...req.body,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},listConnectors(req,res){wrap(repo(req).listConnectors(filters(req)),res)},createCase(req,res){wrap(repo(req).createCase({...req.body,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},closeCase(req,res,id){wrap(repo(req).closeCase(id),res)},createRun(req,res){wrap(repo(req).createRun({...req.body,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},startRun(req,res,id){wrap(repo(req).startRun(id),res)},waitApproval(req,res,id){wrap(repo(req).waitApproval(id,req.body.stepId||''),res)},completeRun(req,res,id){wrap(repo(req).completeRun(id,req.body.output||{}),res)},failRun(req,res,id){wrap(repo(req).failRun(id,req.body.reason||''),res)},rollbackRun(req,res,id){wrap(repo(req).rollbackRun(id,req.body.reason||''),res)},createApproval(req,res){wrap(repo(req).createApproval({...req.body,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},approveApproval(req,res,id){wrap(repo(req).approveApproval(id,req.body.comments||''),res)},rejectApproval(req,res,id){wrap(repo(req).rejectApproval(id,req.body.comments||''),res)},createLog(req,res,runId){wrap(repo(req).createLog({...req.body,runId,tenantId:req.body.tenantId||req.context.tenantId||''}),res,201)},listLogs(req,res,runId){wrap(repo(req).listLogs(runId),res)},metrics(req,res){wrap(repo(req).metrics(req.body.tenantId||req.context.tenantId||''),res)}};
