const{sendJson}=require('../utils/http');function repo(req){return req.context.repositories.privacyRisk}function tenant(req){return req.body.tenantId||req.context.tenantId||''}function wrap(v,res,s=200){Promise.resolve(v).then(data=>sendJson(res,s,{data})).catch(e=>sendJson(res,e.status||500,{error:{code:e.code||'error',message:e.message}}))}module.exports={createFinding(req,res){wrap(repo(req).createFinding({...req.body,tenantId:tenant(req)}),res,201)},planRemediation(req,res,id){wrap(repo(req).planRemediation(id,req.body.owner,req.body.plan,req.body.dueAt),res)},requestException(req,res,id){wrap(repo(req).requestException(id,req.body.reason,req.body.expiresAt),res)},approveException(req,res,id){wrap(repo(req).approveException(id,req.body.approver||req.context.userId),res)},resolveFinding(req,res,id){wrap(repo(req).resolveFinding(id,req.body.note),res)},metrics(req,res){wrap(repo(req).metrics(tenant(req)),res)}};
