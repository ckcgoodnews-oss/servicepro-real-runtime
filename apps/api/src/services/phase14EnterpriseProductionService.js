const { validationError } = require('../errors/domainError');
const ALLOWED_STATUSES=['draft','active','paused','completed','retired','failed'];
const DOMAINS=["high-availability", "zero-downtime-deployments", "kubernetes-operations", "horizontal-scaling", "multi-cloud-deployment", "regional-replication", "disaster-recovery-automation", "security-hardening", "soc-2-automation", "iso-27001-automation", "hipaa-extensions", "pci-dss-extensions", "fedramp-readiness", "penetration-testing-automation", "production-optimization", "version-2-upgrade-tooling", "enterprise-installer", "long-term-support", "release-candidate", "version-2-0-general-availability"];
function normalizeRecord(input={}){if(!input.tenantId)throw validationError('tenantId is required');if(!input.domain||!DOMAINS.includes(input.domain))throw validationError('valid domain is required');if(!input.name)throw validationError('name is required');return {tenantId:input.tenantId,domain:input.domain,name:input.name,status:input.status||'draft',owner:input.owner||'',details:input.details||{}};}
function transitionRecord(row,action,at=new Date().toISOString()){const map={activate:'active',pause:'paused',complete:'completed',retire:'retired',fail:'failed'};if(!map[action])throw validationError(`Unsupported transition: ${action}`);return {...row,status:map[action],updatedAt:at};}
function metrics(rows=[]){return {total:rows.length,active:rows.filter(x=>x.status==='active').length,completed:rows.filter(x=>x.status==='completed').length,failed:rows.filter(x=>x.status==='failed').length,byDomain:Object.fromEntries(DOMAINS.map(d=>[d,rows.filter(x=>x.domain===d).length]))};}
module.exports={ALLOWED_STATUSES,DOMAINS,normalizeRecord,transitionRecord,metrics};
