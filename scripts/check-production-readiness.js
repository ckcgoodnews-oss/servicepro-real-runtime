const { getRepositories, resetRepositoriesForTest } = require('../apps/api/src/repositories/repositoryFactory');
async function main(){resetRepositoriesForTest(); const repos=getRepositories(); const tenantId=process.env.DEFAULT_TENANT_ID||'tenant_demo'; const environmentId=process.env.ENVIRONMENT_ID||''; const environment=environmentId?await repos.operations.findEnvironmentById(tenantId,environmentId):(await repos.operations.listEnvironments(tenantId,{environmentType:'production'}))[0]; if(!environment){console.error('No production environment found. Run npm run seed:operations or create an environment first.'); process.exit(1);} const report=await repos.operations.readiness(tenantId,environment.id,process.env.RELEASE_ID||''); if(repos.store.close) await repos.store.close(); console.log(JSON.stringify(report,null,2)); if(!report.summary.ready) process.exit(2);}
main().catch(err=>{console.error(err);process.exit(1);});
