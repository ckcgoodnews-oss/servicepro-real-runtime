const CONFLICT_RESOLUTION_STRATEGIES = {
  SERVER_WINS: 'server_wins',
  CLIENT_WINS: 'client_wins',
  MERGE_FIELDS: 'merge_fields',
  MANUAL: 'manual'
};

module.exports = { CONFLICT_RESOLUTION_STRATEGIES };
